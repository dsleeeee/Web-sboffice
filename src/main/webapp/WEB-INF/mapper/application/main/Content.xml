<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.solbipos.application.main.content.service.impl.ContentMapper">

  <select id="getNotice" parameterType="sessionInfoVO" resultType="DefaultMap">
  <![CDATA[
  /* contentMapper.getNotice */
  SELECT BOARD_CD, START_DATE, END_DATE,
         TITLE, NOTICE_YN, APPR_FG,
         TARGET_PART_FG, TARGET_SECT, TARGET_TEAM,
         VIEW_CNT, ANSWER_CNT, USE_YN, CONTENT,
         TO_CHAR(TO_DATE(SUBSTR(REG_DT, 0, 8)), 'YYYY.MM.DD') REG_DT, REG_ID
    FROM TB_WB_BOARD_INFO
   WHERE START_DATE <= SYSDATE
     AND END_DATE >= SYSDATE
     AND NOTICE_YN = 'Y'
     AND APPR_FG = '001'
     AND USE_YN = 'Y'
   ORDER
      BY REG_DT DESC
  ]]>
  </select>



</mapper>