<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.solbipos.application.persistence.com.BkmkMapper">

	<!-- 즐겨찾기관리 관리 -->
    <update id="saveBkmk" parameterType="bkmkVO">
        /* BkmkMapper.saveBkmk */
        MERGE INTO
		    TB_WB_BKMK twb
		        USING DUAL ON
		    (
		        USER_ID =# {userId}
		        AND RESRCE_CD =# {resrceCd}
		    )
		    WHEN MATCHED THEN UPDATE
		    SET
		        USE_YN = #{useYn},
		        REG_DT = #{regDt},
		        REG_ID = #{userId}
		        WHEN NOT MATCHED THEN INSERT
		            (
		                USER_ID,
		                RESRCE_CD,
		                USE_YN,
		                REG_DT,
		                REG_ID
		            ) VALUES (
		                #{userId},
		                #{resrceCd},
		                #{useYn},
		                #{regDt},
		                #{regId}
		            )
    </update>

</mapper>